package language.basics;

public class NonAccessModifiers {

	//Even though we are modify the behaviour of data by using non-access modifier but rules of access modifiers still intact..
	
	public static void main(String[] args) {
		//1. static --> can use before method,before data -->make properties are global properties
		//2. abstract --> can use before method,before class,but before the data we can't use --> 
		//3. final --> before class, before methods, before data
		//4. synchronized --> before method/ block ==> 
	}

}
